<div class="row">
  <div class="col-md-8">
    <h1>All Wikis</h1>
  </div>
  <div class="col-md-4">
    <%= link_to "New Wiki", new_wiki_path, class: 'btn btn-success' %>
  </div>
</div>

<br>

<div class='row'>
  <div class='col-md-8'>
    <% @wikis.each do |wiki| %>
      <h4><%= link_to wiki.title, wiki %></h4>
      <h6>Published <%= time_ago_in_words(wiki.created_at) %> ago by <%= wiki.user.username %></h6>
      <% if wiki != @wikis.last %>
      &nbsp;
      <br>
        -----
      <% end %>
    <% end %>
  </div>
</div>
